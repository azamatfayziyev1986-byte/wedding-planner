services:
  - type: web
    name: wedding-planner-bot
    env: node
    plan: free
    buildCommand: npm run postinstall
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: PORT
        value: 5000
